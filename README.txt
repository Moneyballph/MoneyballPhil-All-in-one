Moneyball Phil — All-in-One Simulator

Run locally:
  pip install -r requirements.txt
  streamlit run app.py

Background image is in assets/mbp_bg.png. Replace it with your preferred background if needed.
